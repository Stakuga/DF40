# Runtime dependencies for model_run.py (CLIP-large deepfake classifier + explanations).
# Versions are those the framework was validated with; nearby versions should also work.

torch==2.7.1
torchvision==0.22.1
transformers==4.52.4
numpy==2.3.5
opencv_python==4.6.0.66
Pillow==12.0.0
PyYAML==6.0.3
scikit_learn==1.7.2
grad_cam==1.5.5          # provides the `pytorch_grad_cam` package (Score-CAM)
google-genai             # textual explanations via Gemini 2.5 Pro
